# Baseline/coarse profile for measurement. 0.01 matches OMPL's conventional default fraction.
# It is an evaluation setting, not a final safety certification value.
planning_plugin: ompl_interface/OMPLPlanner
request_adapters: >-
  default_planner_request_adapters/AddTimeOptimalParameterization
  default_planner_request_adapters/ResolveConstraintFrames
  default_planner_request_adapters/FixWorkspaceBounds
  default_planner_request_adapters/FixStartStateBounds
  default_planner_request_adapters/FixStartStateCollision
  default_planner_request_adapters/FixStartStatePathConstraints
start_state_max_bounds_error: 0.1

planner_configs:
  RRTConnectkConfigDefault:
    type: geometric::RRTConnect
    range: 0.0

torso: &baseline_group
  planner_configs: [RRTConnectkConfigDefault]
  longest_valid_segment_fraction: 0.01
left_arm: *baseline_group
right_arm: *baseline_group
left_gripper: *baseline_group
right_gripper: *baseline_group
left_arm_with_torso: *baseline_group
right_arm_with_torso: *baseline_group
dual_arm: *baseline_group
dual_arm_with_torso: *baseline_group
whole_body: *baseline_group
